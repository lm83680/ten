import 'package:flutter/widgets.dart';

class TenButton extends StatelessWidget {
  const TenButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}